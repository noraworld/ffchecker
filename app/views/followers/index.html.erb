<% if @users %>
  <% @users.each do |user| %>
    <span><%= user['screen_name'] %></span>
    <input class="follow" type="button" name="<%= user['screen_name'] %>" value="フォロー">
    <br>
  <% end %>
<% else %>
  <p>ユーザは存在しません</p>
<% end %>

<% if @users %>
  <% @users.each do |user| %>
    <span><%= user['screen_name'] %></span>
    <input class="unfollow" type="button" name="<%= user['screen_name'] %>" value="フォロー解除">
    <br>
  <% end %>
<% else %>
  <p>ユーザは存在しません</p>
<% end %>
